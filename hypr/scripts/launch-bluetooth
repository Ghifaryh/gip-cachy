#!/bin/bash
CLASS="bluetui-float"

# Toggle off if already open
if pgrep -f "kitty --class=$CLASS" >/dev/null; then
  pkill -f "kitty --class=$CLASS"
  exit 0
fi

# Launch TUI window directly
kitty --class="$CLASS" -e bluetui
